# ML Command Center (Experimental)

This is an opt-in editor plugin for experimenting with ML/RL monitoring and
controls. It is not enabled by default in the test project.

The current implementation uses placeholder runner entries and can be tested
with `mock_rl_runner.gd`. It is UI scaffolding, not a stable training API.
